Imports System.Drawing.Drawing2D

Module varGlobales

    'CONEXION
    'Public cadenaConexion As String = "Data source =JEISSONPC\SQLEXPRESS; Initial Catalog=bdHppTaller3; integrated security = true "
    Public cadenaConexion As String = "Data source =YERCKEN\SQLEXPRESS; Initial Catalog=bdHppTaller3; integrated security = true"

    'ESTILO BOTÓN----------------

    'Public Function degradado(width As Integer, height As Integer, color1 As Color, color2 As Color) As Image

    '    Dim gradientBrush As New LinearGradientBrush(New Rectangle(0, 0, width, height), color1, color2, LinearGradientMode.Vertical)
    '    Dim bitmap As New Bitmap(width, height)
    '    Dim graphics As Graphics = Graphics.FromImage(bitmap)
    '    graphics.FillRectangle(gradientBrush, 0, 0, width, height)
    '    gradientBrush.Dispose()
    '    graphics.Dispose()
    '    Return bitmap

    'End Function



End Module
